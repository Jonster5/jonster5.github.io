{
  "name": "sophia",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "bun run build-sunshine.ts",
    "build": "vite build",
    "postbuild": "git add .;git commit;git push origin main;",
    "preview": "vite preview",
    "predeploy": "bun run build",
    "deploy": "git subtree push --prefix dist origin gh-pages",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.1",
    "@tsconfig/svelte": "^5.0.4",
    "@types/bun": "^1.2.21",
    "@types/node": "^24.3.0",
    "arktype": "^2.1.20",
    "svelte": "^5.38.1",
    "svelte-check": "^4.3.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "marked": "^16.2.1"
  }
}
